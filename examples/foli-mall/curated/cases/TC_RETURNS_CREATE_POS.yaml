case_type: single
test_id: TC_RETURNS_CREATE_POS_001
relevance_id: api_api_returns_post
tag: P0
api_name: 创建退货退款申请（order-fixture 造已完成订单）
app_name: foliMail
method: POST
url: /api/returns
request_head:
  Content-Type: application/json
  Authorization: "Bearer #{buyer01}"
request_body:
  returnReason: e2e-return-reason
  returnType: 1
status_code: 200
preprocessors:
  - name: order-fixture
    config:
      order_status: 4
assert_dict:
  code: '100000'
assert_rules:
  - $.data.id is_not_null
  - $.data.returnNo is_not_null
  - $.data.orderId is_not_null
  - $.data.status == 0
  - $.data.refundAmount > 0
