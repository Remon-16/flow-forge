case_type: single
test_id: TC_CART_GET_BOUND_002
relevance_id: api_api_cart_get
tag: P1
api_name: 获取购物车列表（空购物车，边界）
app_name: foliMail
method: GET
url: /api/cart
request_head:
  Content-Type: application/json
  Authorization: "Bearer #{buyer01}"
request_body: {}
status_code: 200
preprocessors:
  - name: cart-fixture
    config:
      mode: clear
assert_dict:
  code: '100000'
assert_rules:
  - $.data.length() == 0
