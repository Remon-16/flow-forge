case_type: single
test_id: TC_ORDER_CREATE_POS_001
relevance_id: api_api_orders_post
tag: P0
api_name: 创建订单（余额复位 + 购物车造数）
app_name: foliMail
method: POST
url: /api/orders
request_head:
  Content-Type: application/json
  Authorization: "Bearer #{buyer01}"
request_body:
  receiverName: 张三
  receiverPhone: "13800001111"
  receiverAddress: 北京市朝阳区
status_code: 200
preprocessors:
  - name: balance-fixture
    config:
      balance: 10000
  - name: cart-fixture
    config:
      mode: ensure
      product_id: 1000000000000003001
      quantity: 1
assert_dict:
  code: '100000'
assert_rules:
  - $.data[0].id is_not_null
  - $.data[0].orderNo is_not_null
  - $.data[0].status == 1
  - $.data[0].totalAmount > 0
