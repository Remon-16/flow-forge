scriptType: APITest
envName: local
caseFilePath: ./test_cases.xlsx
maxThread: 5
reportName: APIReport

# ---------------------------------------------------------------------------
# 敏感数据可以在此配置。
# 如果有任意的自定义处理器，则会自动传递到处理器的 global_config 中，无需手工处理。
# ---------------------------------------------------------------------------
processor_configs: {}
# 示例（用户按需填写）：
# processor_configs:
#   hmac-sign:
#     secret_env: SIGN_SECRET
#     algorithm: sha256
#   sql-cleanup:
#     host: localhost
#     port: 3306
#     database: testdb
